import ClientList from './ClientList'

export const metadata = {
  title: 'Guild List | Admin',
  description: 'Manage Guild list',
}

export default function GuildListPage() {
  return <ClientList />
}